using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Cat_StateMachine : MonoBehaviour
{
    enum States
    {
        Idle,
        Approach,
        DemonicBite,
        TailSweep,
        Jump,
        PawPunch
    }

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
